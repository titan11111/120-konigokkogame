# 120-konigokkogame アップデート計画書 v2「5時のチャイム作戦」

> 4段ゲートの **② 設計書**。実装はこの文書を承認してから着手する。
> 作成: 2026-08-30 ／ 対象: `120-konigokkogame/index.html`
> 方針: **世界観は1ミリも変えない**。「小学生が5時のチャイムまでに家へ帰る」まま、
> メタルギアソリッドの潜入メソッドを子どもの語彙に翻訳して深さを足す。

---

## 0. 現状（コード実測・2026-08-30）

| 項目 | 実測値 | 根拠 |
|---|---|---|
| 実体 | `index.html` 単一 895行 / 38.6KB | フォルダ計 296KB（.git込み）→ 20MB鉄則に対し **余裕99%** |
| マップ | 44×30タイル（TS=32px）= 1408×960px、**1面のみ** | `MW=44, MH=30` |
| 敵 | 4体。**挙動は全員同一**（巡回ルートと速度だけ違う） | `makeEnemy()` は種別を持たない |
| 隠れ場所 | 草むら40 + ダンボール14 = **54点。すべて「点」** | `BUSH_T` / `BOX_T` |
| 移動 | しのび足62 / 歩き132 / ダッシュ205 px/s | `const spd = ...` |
| 視界 | FOV 1.15rad（約66°）／ 到達230px | `FOV`, `RANGE` |
| 警戒 | `patrol → suspect → chase` の3状態。**個体ごと。町全体の警戒度は無し** | `e.mode` |
| 操作 | 仮想スティック＋2ボタン（ダッシュ／しのび足） | `#stick`, `#btnRun`, `#btnSneak` |
| 音 | WebAudioの素のオシレータのみ（square/sine/triangle） | `tone()` |
| 既にあるMGS要素 | ダンボール変装／ソリトン風レーダー／`!``?`マーク／FOXHOUNDランク | 良い土台。**全部活かす** |

**評価**: 骨格は既に良質。足りないのは「深さ」ではなく **選択肢**。
いまプレイヤーが取れる行動は「歩く・走る・しゃがむ・隠れる」の4つしかなく、
**敵に対して働きかける手段がゼロ**。石投げの追加が、この作品を別物に変える一手になる。

---

## 1. このアップデートの到達点

```
1面 → 5面（目的が5種類すべて違う）
敵1種 → 敵8種（＋ボス1体）
隠れ場所「点」54個 → 「点・線・空間」の3層
プレイヤーの手札 4つ → 9つ（石投げ・誘導・ひるませ・隠れ移動 が加わる）
警戒 個体ごと → 町全体の4段階フェーズ
```

**変えない核**: 見つからずに帰るのが基本。戦闘は例外。鬼は倒せない（子どもだから）。

---

## 2. MGSメソッド翻訳表 ← この計画の心臓部

MGSの仕組みをそのまま持ち込むと世界観が壊れる。**必ず子どもの語彙に翻訳する。**

| MGSの仕組み | 本作での翻訳 | ねらい |
|---|---|---|
| 壁ノックで誘導 | **石を投げて音を出す** | 手札ゼロ問題の解決。最重要 |
| ALERT→EVASION→CAUTION | **「みつかった！」→「さがしてる」→「ようじん中」** | 見つかっても即終了でなく、代償が続く緊張 |
| 段ボール箱 | すでに実装済み（`われもの`） | **動くとバレる**ルールを追加して本物にする |
| ソリトンレーダー | すでに実装済み | CAUTION中は砂嵐で見えづらくする |
| 無線（CODEC） | **ママからのケータイ／お兄ちゃんのトランシーバー** | 面の導入・目的説明・ヒント |
| 気絶体の発見でALERT | **石で泣かせた鬼を仲間が見つける** | ひるませ行動にリスクを持たせる |
| 足音の材質差 | **アスファルト／砂利／落ち葉／水たまり** | しのび足に意味を与える |
| 固定センサー・カメラ | **電柱のカラス／飼い犬のポチ／自転車のベル** | 「動かない脅威」の追加 |
| ダクト移動 | **公園の土管／用水路の溝／生け垣の裏** | 隠れながら"移動できる"場所 |
| ボス戦（弱点パターン） | **ガキ大将の帽子を石で落とす** | 唯一の攻撃対象。倒さず「降参させる」 |
| クリアランク | すでに実装済み（FOXHOUND級） | 面ごとに集計 |

---

## 3. アラートフェーズ（町全体の4段階）← 一番効く改修

現状は敵が個別に `chase` するだけなので、逃げ切った瞬間に何事もなかったことになる。
MGSの緊張感の正体は **「見つかった代償が、その後ずっと続く」** ことにある。

| フェーズ | 表示 | 継続 | 鬼の行動 | 世界の変化 |
|---|---|---|---|---|
| **ALERT** | 「みつかった！」 | 20秒 | 全鬼が目撃地点へ集結。速度1.9倍 | 画面赤み・レーダー赤・BGM停止 |
| **EVASION** | 「さがしてる…」 | 30秒 | 目撃地点を中心に散開して捜索 | 隠れ場所を覗きに来る |
| **CAUTION** | 「ようじん中」 | 45秒 | 通常巡回に戻るが **視界+15% / 速度+10%** | レーダーに砂嵐。街灯が増える |
| **NORMAL** | 「しのび中」 | — | 通常巡回 | 通常 |

- タイマーは **隠れている間も進む**（逃げ切りを許す）
- ただし **ALERT中に一度でも視認されるとタイマーがリセット**（粘着される）
- クリアランクは「最終フェーズがNORMALだったか」で大きく変動 → 静かに帰る動機になる

---

## 4. 5面の設計 ── 目的をすべて変える

物語は一本道：**公園 → 商店街 → 路地 → 神社 → 河川敷 → おうち**。

| 面 | 舞台 | 目的（ミッション） | 新要素 | 制限時間 |
|---|---|---|---|---|
| **1** | 夕方の公園 | **脱出**：公園の出口までたどり着く | 石投げチュートリアル・隠れ方 | 120秒 |
| **2** | 商店街 | **回収**：落とした給食袋・体操着・上ばきの **3つを拾ってから** 出口へ | 犬（ポチ）／のれん／自販機の陰 | 180秒 |
| **3** | 住宅街の路地 | **同行**：泣いている1年生を連れて帰る（連れは足が遅く、音を出す） | 護衛AI・「まってて」指示 | 180秒 |
| **4** | 神社の裏 | **潜入奪取**：鍵を持つ鬼から **鍵を取り返して** 門を開ける | 懐中電灯鬼・カラス見張り・背後からのスリ | 200秒 |
| **5** | 河川敷 → 自宅前 | **ボス**：ガキ大将の帽子を落として降参させ、玄関へ | ボス戦・子分の増援 | 240秒 |

**設計意図**: 1面=覚える／2面=探索が増える／3面=足かせが増える／4面=能動的行動が要る／5面=全部使う。
5面すべて「見つからないのが最適解」は共通。3面の連れと5面のボスだけが例外的に交戦要素を持つ。

---

## 5. 敵キャラ図鑑（8種＋ボス）

| # | 名前 | 視界 | 聴覚 | 速度 | 特徴 | 弱点 | 初出 |
|---|---|---|---|---|---|---|---|
| 1 | **見回り鬼** | 66° / 230px | 標準 | 78 | 現行の基本鬼 | 石で1.2秒ひるむ | 1面 |
| 2 | **早足鬼** | 50° / 180px | 弱い | 130 | 追跡がしつこい。視野は狭い | 曲がり角で撒ける | 1面 |
| 3 | **地獄耳** | 40° / 140px | **2倍** | 70 | 歩きの足音も拾う。しのび足必須 | 石の音に**必ず**釣られる | 2面 |
| 4 | **ポチ（犬）** | 30° / 100px | 嗅覚 | 105 | プレイヤーの通った跡を数秒追う | 石を投げると追いかけて数秒消える | 2面 |
| 5 | **カラス（見張り）** | 全方位 / 110px | — | 固定 | 電柱の上。範囲に入ると鳴いて周囲を CAUTION に | 石を当てると飛び去る（永久排除） | 4面 |
| 6 | **懐中電灯鬼** | **25° / 380px** | 標準 | 74 | 細く長い光。暗闇が効かない | 光の外は完全に見えない | 4面 |
| 7 | **二人組** | 66° / 230px ×2 | 標準 | 78 | 背中合わせで歩き死角が無い | 石で片方だけ引き離す | 3面 |
| 8 | **子分** | 66° / 200px | 標準 | 90 | ボスが呼ぶと湧く。無限 | 相手にせず走るのが正解 | 5面 |
| **B** | **ガキ大将** | 90° / 260px | 標準 | 95 | HP=帽子3回。突進攻撃 | **背後 or 突進の直後に帽子へ石** | 5面 |

**ルール**: 鬼は誰も「倒せない」。ひるませる・撒く・追い払うだけ。子どもの鬼ごっこだから。

---

## 6. 石投げの仕様

### 基本
- 所持 **3個**。地面のキラキラした小石から拾える（面に8〜12個配置、リポップなし）
- **放物線**で飛ぶ。飛距離は最大 260px、長押しで飛距離を伸ばす
- 照準中は **着弾予測点（点線＋◎）** を表示。照準中はプレイヤー移動速度が半分

### 3つの用途
| 用途 | 条件 | 効果 |
|---|---|---|
| **撹乱（本命）** | 地面・壁に着弾 | 着弾点から半径 **170px** の鬼が `suspect` になり **着弾点へ歩いてくる**。最強の道具 |
| **ひるませ** | 鬼に直撃 | **1.2秒行動不能**（目をおさえる）。ただし当てた鬼は `suspect` になる＝タダではない |
| **ボス攻撃** | ボスの帽子に命中 | 帽子ダメージ1。正面はガードされる。**背後 or 突進直後の隙**のみ有効 |

### 操作
| | PC | モバイル |
|---|---|---|
| 照準 | 右クリック長押し / `X`長押し ＋ 方向キー | **投擲ボタン長押し** → ドラッグで方向 |
| 発射 | 離す | 離す |
| キャンセル | 右クリック / `Esc` | ボタン外へスライドして離す |

**設計判断**: 石は「攻撃手段」ではなく **「鬼をどかす手段」** として設計する。
プレイヤーが強くなるのではなく、**通れなかった道が通れるようになる**のが正しい快感。

---

## 7. 隠れ場所の拡張 ── 点 → 線 → 空間

現状の54個はすべて「点」。**そこに立てば消えるが、そこから動けない。**
これが現行版の一番の窮屈さなので、3層に拡張する。

| 層 | 種類 | 挙動 | 新規 |
|---|---|---|---|
| **点** | 草むら / ダンボール | 立つと消える。**動くとバレる（MGSルール追加）** | 既存＋ルール変更 |
| **点** | 自販機の陰 / 看板の裏 / 電柱 | **方向依存**。鬼と反対側にいる時だけ隠れる | ★新規 |
| **線** | **生け垣ライン / 用水路の溝** | **隠れたまま移動できる**。速度は歩きの70% | ★新規・最重要 |
| **線** | のれん / 店の軒下 | 通り抜けられる。中は見えない | ★新規 |
| **空間** | **公園の土管 / 車の下** | 入るのに0.8秒。中は完全に安全。**出口が2つ** | ★新規 |

**「線」の追加が最大の改善。** 隠れながら前進できる＝ステルスが受け身でなくなる。

---

## 8. 操作性の改善 ── 現状の具体的な不満点と対策

コードを読んで特定した実際の問題。推測ではなく該当箇所を明記する。

| # | 現状の問題 | 該当 | 対策 |
|---|---|---|---|
| 1 | **カメラが後追い**。ダッシュ中に前が見えない | `cam.x += (tx-cam.x)*dt*6` | 進行方向へ **最大90pxの先読みオフセット**。ダッシュ時ほど大きく |
| 2 | **モードが押しっぱなし**。しのび足を10秒続けると指が疲れる | `keys['z']` | **トグル切替をオプション化**（設定は localStorage に保存） |
| 3 | 壁の**角にひっかかる** | `moveCircle()` の軸分離のみ | **コーナーコレクション**：1タイル未満のズレなら滑らせて回り込む |
| 4 | スティックに**デッドゾーンが無い**ので微振動で歩く | `touch.dx = dx/max` | デッドゾーン12% ＋ 入力カーブ（低速域を細かく） |
| 5 | ボタンが2つ。**石投げが入ると足りない** | `#btnRun`,`#btnSneak` | 右下を **3ボタン扇形配置**（ダッシュ／しのび／石）に再設計 |
| 6 | **一時停止できない** | 無し | `Esc` / 一時停止ボタン。ミッション目標を再確認できる画面に |
| 7 | しのび足62px/sは**遅すぎて退屈** | `spd` | **78px/s** に引き上げ。代わりに完全無音を維持 |
| 8 | **safe-area 未対応**。iPhoneのホームバーにボタンが被る | CSS | `env(safe-area-inset-*)` を全UIに適用（`/ios-game-base` 準拠） |
| 9 | つかまった時に**最寄りの隠れ場所へワープ**するのが唐突 | `caught()` | ワープではなく **その場で2.2秒無敵＋全鬼が一時的に散開**。理不尽さを消す |
| 10 | 目的が**画面上に常時出ていない** | HUD | HUD上部に **現在のミッション目標＋進捗**（例:「給食袋 2/3」）を常時表示 |

---

## 9. 音の方針 ── ビープ廃止 ／ 発見音は残す

> タイタン指示（2026-08-30）:「ビープ音は取って。見つかった時の音は出したい」

| 音 | 現状 | 変更後 |
|---|---|---|
| **足音** | `sfx.step()` = 150Hzの正弦波ビープ。**歩くたびにピコピコ鳴って耳障り** | **廃止**。ノイズ源＋短いローパスで「サッ」という布擦れ／砂利音に差し替え。しのび足は完全無音 |
| **隠れた瞬間** | `sfx.hide()` = 320Hzビープ | **廃止**。葉ずれのノイズ音に |
| **あやしまれた** | `sfx.notice()` = 760Hzビープ | **控えめに残す**。単音ビープ→木質の「コッ」に |
| **見つかった** | `sfx.alert()` = 2音ビープ | **★残す・むしろ強化**。MGSの「!」に相当する作品の顔。<br>「みぃつけた！」の掛け声＋緊迫音を明確に鳴らす |
| **つかまった** | `sfx.caught()` | 残す |
| **石の着弾** | 無し | ★新規：カツンという硬い単発音（誘導の手応え） |
| **5時のチャイム** | `sfx.chime()` | 残す。ラスト30秒から遠くで予鈴を鳴らす |

**原則**: 常時鳴る音はビープにしない（疲れる）。**一度きりの重要イベントだけ音を立てる。**
とくに「見つかった瞬間」は最も鳴らすべき音として設計する。

---

## 10. 技術設計

### ファイル構成（分割を推奨）
現行895行に5面・敵8種・石・ボスを足すと **3000行超**。単一ファイルでは保守不能になる。

```
120-konigokkogame/
├── index.html          # DOM・CSS・起動のみ（〜250行）
├── SPEC.md             # 完成後に確定仕様として作成
├── UPDATE-PLAN.md      # 本書
└── js/
    ├── main.js         # ループ・状態遷移
    ├── config.js       # 定数（速度・視界・時間）
    ├── levels.js       # ★5面のマップデータ
    ├── enemy.js        # ★敵8種の定義とAI
    ├── player.js       # 移動・隠れ・石
    ├── stone.js        # ★石の弾道と着弾
    ├── alert.js        # ★4段階フェーズ管理
    ├── render.js       # 描画・ライティング
    ├── hud.js          # HUD・レーダー
    └── audio.js        # ★音の作り直し
```

⚠️ **注意**: ES modules 化すると `file://` の直開きが不可になる。
`_tools/preview.sh`（python3 http.server）と GitHub Pages はどちらもHTTP配信なので**実害なし**。
これは実装前に確認いただきたい唯一の判断点。

### 面データのフォーマット
```js
{ id:1, name:'夕方の公園', time:120,
  objective:{ type:'escape', goal:{x:1,y:4,w:4,h:1} },
  size:{w:44,h:30},
  buildings:[...], hides:[{type:'hedge', from:[3,8], to:[9,8]}],
  stones:[[5,12],[20,7]],
  enemies:[{type:'patrol', route:[[13,5],[13,16]]}],
  codec:'ママ「5時のチャイムまでに帰ってきなさいよ！」' }
```
目的タイプは `escape` / `collect` / `escort` / `steal` / `boss` の5種。

---

## 11. 実装フェーズ（この順に、1フェーズ＝1コミット）

| Ph | 内容 | 完了条件 |
|---|---|---|
| **1** | ファイル分割＋面データ構造化。**現行1面をデータとして再現** | 見た目・挙動が現行と同一。デグレゼロ |
| **2** | 操作性10項目＋音の作り直し（§8・§9） | ビープ消失を実機で確認。発見音が鳴る |
| **3** | 石投げ＋撹乱＋アラート4フェーズ（§3・§6） | 石で鬼を誘導して通れる。フェーズがHUDで遷移する |
| **4** | 敵8種＋隠れ場所3層（§5・§7） | 全種が1面以上に登場。生け垣を隠れて移動できる |
| **5** | 5面＋目的5種＋ボス戦（§4） | 1面から5面まで通しでクリアできる |

**各フェーズ後に必ず実施**: `_tools/preview.sh 120-konigokkogame` で目視 → `node _tools/audit.mjs 120-konigokkogame` → `_tools/game-harness.sh` → コミット。

---

## 12. 完了条件（この全部が○になるまで「完成」と言わない）

- [ ] 1面から5面まで通しでクリアできる（実際に自分でプレイして到達）
- [ ] 5面それぞれの目的が異なり、HUDに常時表示される
- [ ] 石で鬼を誘導して、正面突破できない場所を通過できる
- [ ] 敵8種すべてが登場し、それぞれ別の対処法が要る
- [ ] 生け垣・溝を**隠れたまま移動**できる
- [ ] 足音のビープが鳴らない／見つかった時の音が鳴る（実機で耳で確認）
- [ ] iPhone実機でボタンがホームバーに被らない
- [ ] 公開実体 20MB以下（現状296KBなので余裕）
- [ ] `audit.mjs` の機械箱が0件
- [ ] `game-harness.sh` が PASS

---

## 13. 非目標（やらないこと）── 鉄則7

- ❌ 他のゲームへの横展開・共通ライブラリ化（頼まれていない）
- ❌ 画像アセットの追加（現行の全Canvas描画を維持。20MB鉄則にも効く）
- ❌ 外部ライブラリの導入（現状ゼロ依存を維持）
- ❌ 鬼を「倒せる」ようにすること（世界観が壊れる。ひるませるだけ）
- ❌ セーブデータ・スコアランキング・オンライン要素
- ❌ フォルダ番号の変更（`120-konigokkogame` のまま。`120-fumifumi` と番号が重複しているが**触らない**）

---

## 14. リスク

| リスク | 対策 |
|---|---|
| 5面ぶんのマップを手打ちすると膨大 | Ph1でデータ構造を固めてから。1面あたり建物10・隠れ場所30程度に抑える |
| 敵8種でフレーム落ち | BFSは0.5秒に1回・敵ごと。8体でも1面44×30=1320セルなら問題ない見込み。Ph4で実測する |
| 石の弾道が2.5D視点で分かりにくい | 着弾予測点を必ず出す。影で高さを表現 |
| スコープが大きく、途中で息切れ | フェーズごとに**遊べる状態**でコミット。Ph3まででも十分に別ゲームになる |

---

**次の一手**: 本計画書のご承認 → Phase 1（ファイル分割・現行1面の再現）に着手。
判断いただきたいのは §10 の **ES modules 分割（file:// 直開きが不可になる）** の1点のみです。
