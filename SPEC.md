# 120-konigokkogame ─ おうちに帰れ！ 仕様書

> ジャンル: HD-2D 見下ろし潜入アクション（かくれんぼ）
> バージョン: v2「5時のチャイム作戦」（2026-08-30）
> 計画書: `UPDATE-PLAN.md` ／ 旧v1は git 履歴 `90d885c` 参照

## 1. 世界観

小学生が公園からの帰り道、町じゅうにいる「鬼」に見つからずに **5時のチャイムまでに** 家へ帰る。
鬼は **誰も倒せない**。ひるませる・撒く・追い払うだけ。子どもの鬼ごっこだから。

## 2. 勝利条件・敗北条件

| | 条件 |
|---|---|
| 面クリア | その面の目的を果たしてゴール矩形に入る |
| 全クリア | 5面すべてをクリアして自宅の玄関に入る |
| 敗北 | ライフ0（3回タッチされる）／ 制限時間切れ（5時のチャイム） |

## 3. 操作

| 操作 | キーボード | タッチ |
|---|---|---|
| 移動 | ← → ↑ ↓ / WASD | 左半分のバーチャルスティック（デッドゾーン12%＋入力カーブ） |
| ダッシュ | Shift | ダッシュボタン |
| しのび足 | Z / Ctrl | しのび足ボタン |
| 石を構える→投げる | X 長押し→離す | いしボタン 長押し→離す |
| 照準方向 | マウス位置 or 方向キー | スティック |
| アクション（とる／まってて） | E / F | アクションボタン |
| 一時停止 | Esc / P | 右上 ❚❚ |

- **トグル操作**: ダッシュ／しのび足を「押しっぱなし」ではなく「切りかえ」にできる（タイトル画面のチェック、`localStorage: kg_toggle` に保存）
- カメラは進行方向へ最大90px先読みする

## 4. プレイヤー

| 状態 | 速度 | 足音の届く距離 |
|---|---|---|
| しのび足 | 78 px/s | 0（無音） |
| ふつう歩き | 132 px/s | 92 px |
| ダッシュ | 205 px/s | 190 px |

- 石の所持上限 **3個**。地面の小石から拾う（面ごとに8〜12個、リポップなし）
- 石を構えている間は移動速度が半分
- タッチされると **ライフ−1・その場で2.2秒無敵**（ワープはしない）。警戒は CAUTION まで下がる

## 5. 隠れ場所（点・線・空間の3層）

| タイル | 種類 | じっとしている時に見つかる距離 | 動いている時 | 移動速度倍率 | 潜り込み時間 |
|---|---|---|---|---|---|
| くさむら | 点 | 46 | 112 | 0.62 | 0秒 |
| ダンボール | 点 | 40 | **132** | 0.42 | 0秒 |
| いけがき | **線** | 42 | 54 | 0.72 | 0秒 |
| みぞ（用水路） | **線** | 38 | 48 | 0.68 | 0秒 |
| のれん | **線** | 44 | 60 | 0.80 | 0秒 |
| どかん | **空間** | 0（完全に安全） | 0 | 0.50 | 0.5秒 |
| 車の下 | **空間** | 0（完全に安全） | 0 | 0.40 | 0.6秒 |

- **ダンボールは動くと極端にバレる**（MGSルール）。線の隠れ場所は隠れたまま移動する用に作ってある
- 自販機・看板・電柱（PROP）は視線を遮る **設置物**。反対側に立てば自動的に見つからない（方向依存の遮蔽）

## 6. 石

- 飛距離: 溜め0で96px、最大で268px。**着弾点は計算で確定**するので照準ガイドは必ず当たる
- **撹乱（本命）**: 着弾点から半径 **170px** の鬼が `suspect` になり、着弾点へ歩いてくる。**視線不要＝角を曲がって届く**
- **ひるませ**: 鬼に直撃で **1.2秒** 行動不能。ただし当てた鬼は `suspect` になる（タダではない）
- **カラス**: 石を当てると飛び去り、**永久に排除**できる
- **ボス**: 正面はガードされる。**突進の直後（recover）または背後（角度差2.0rad超）** のみ帽子に当たる

## 7. 町ぜんたいの警戒フェーズ

| フェーズ | 表示 | 継続 | 視界倍率 | 速度倍率 | 鬼の行動 |
|---|---|---|---|---|---|
| ALERT | みつかった！ | 20秒 | 1.25 | 1.30 | 全鬼が目撃地点へ集結 |
| EVASION | さがしてる… | 30秒 | 1.12 | 1.18 | 目撃地点の周辺に散開して捜索 |
| CAUTION | ようじん中 | 45秒 | 1.15 | 1.10 | 通常巡回。レーダーに砂嵐 |
| NORMAL | しのび中 | — | 1.00 | 1.00 | 通常巡回 |

時間で1段ずつ下がる。ALERT中に再び視認されるとタイマーがリセットされる。

## 8. 鬼（8種＋ボス）

| 名前 | 視野角 | 視程 | 速度 | 耳 | 特徴 | 初出 |
|---|---|---|---|---|---|---|
| 見回り鬼 | 1.15 rad | 230 | 78 | 1.0 | 基本 | 1面 |
| 早足鬼 | 0.88 | 180 | 118 | 0.7 | 追跡がしつこい／視野は狭い | 3面 |
| じごく耳 | 0.72 | 145 | 72 | **2.2** | 歩きの足音も拾う | 2面 |
| ポチ（犬） | 0.55 | 105 | 104 | 1.4 | **足あとを追う**（7秒ぶん） | 2面 |
| みはりガラス | 全方位 | 112 | 固定 | — | 鳴くと町が CAUTION／EVASION へ | 4面 |
| ライト鬼 | **0.46** | **378** | 74 | 1.0 | 細く長い光。暗闇が効かない | 4面 |
| ふたり組 | 1.15 ×2 | 230 | 78 | 1.0 | 背中合わせで死角なし | 3面 |
| こぶん | 1.15 | 200 | 92 | 1.0 | ボスが呼ぶ | 5面 |
| **ガキ大将** | 1.55 | 260 | 96 | 1.0 | 帽子3回。突進（予備0.75秒→突進0.85秒→スキ1.45秒） | 5面 |

状態遷移: `patrol → suspect → chase`（＋ `stun`）。発見メーターは 1.55/秒 で上昇、0.70/秒 で減衰。
ダッシュ中は1.7倍見つかりやすく、しのび足中は0.72倍。

## 9. 5つの面

| 面 | 名前 | 目的タイプ | 目的 | 制限 | 敵 |
|---|---|---|---|---|---|
| 1 | ゆうがたの公園 | `escape` | 公園の出口へ | 120秒 | 見回り×3 |
| 2 | 夕やけ商店街 | `collect` | 落とし物3つを拾って西口へ | 180秒 | 見回り×2・じごく耳・ポチ |
| 3 | 住宅街のろじ | `escort` | 1年生を連れて出口へ | 180秒 | ふたり組・早足・見回り・じごく耳 |
| 4 | 神社のうら | `steal` | 鍵持ちの背後から鍵を取り、門を開ける | 200秒 | ライト×2・見回り×2・じごく耳・カラス×2 |
| 5 | 町なか、あと少し | `boss` | ガキ大将の帽子を3回落として玄関へ | 240秒 | 見回り×4・早足・ライト・カラス・ボス |

- **3面の連れ**: 追従52px、速度112、移動時に半径58pxの音を出す。ゴールには **96px以内** に連れていること
- **4面の奪取**: 鍵持ちの背後（角度差2.0rad超）に30px以内で近づき、アクション
- **4面の鍵持ちマーカー**（2026-08-30追加）: 「どこにいるか分からない」を無くすため、鍵を取るまで居場所を常時提示する
  | 状況 | 出るもの |
  |---|---|
  | 画面内 | 足元に金色のパルス輪／頭上に浮かぶ鍵アイコン／帽子に黒フチ（ライト鬼の帽子 `#e8c14a` と色が近いため） |
  | 画面外 | 画面のふちに 🔑マーカー＋外向き三角＋距離「◯m」 |
  | レーダー | 鍵持ちだけ金色（`#ffb703`）。**範囲外でもリムにクランプ**して菱形で方向を出す |
  | HUD目的欄 | 「かぎ なし・あと◯m」（1m＝1タイル＝32px） |
  - 出すのは **目的の鬼1体だけ**。ほかの鬼の位置は今までどおり自分で探す（潜入の緊張を壊さないため）

## 10. 音（v2で全面的に作り直し）

**方針**: 常時鳴る音をビープにしない。一度きりの重要イベントだけ音を立てる。

| 音 | 実装 |
|---|---|
| 足音 | **ホワイトノイズ＋ローパス**。地面（アスファルト／草／砂利）で音色が変わる。しのび足は完全無音 |
| かくれる | 葉ずれのノイズ |
| あやしまれた | 木質の「コッ」（ノイズ＋短い三角波） |
| **見つかった** | **この作品の顔**。ノイズの一撃＋二段の掛け声＋低音の緊迫音の4層 |
| 石の着弾 | 高域ノイズ＋短い「カツン」 |
| 5時のチャイム | 正弦波の鐘（世界観の要のため音程を残す）。残り30秒で予鈴 |

v1にあった `sfx.step()` の150Hz正弦波ビープ（歩くたびに鳴る）は **廃止**。

## 11. ファイル構成

```
120-konigokkogame/
├── index.html      DOM・CSS・起動のみ
├── SPEC.md         本書
├── UPDATE-PLAN.md  v2の設計計画
├── .nojekyll
└── js/             ES modules（HTTP配信が必要。file:// 直開きは不可）
    ├── main.js     進行・面遷移・目的判定
    ├── config.js   チューニング値の一元管理
    ├── levels.js   5面のデータ
    ├── map.js      グリッド構築・当たり判定・視線・BFS
    ├── player.js   移動・かくれる・足音・連れ
    ├── enemy.js    鬼8種＋ボスのAI
    ├── stone.js    石の弾道と着弾
    ├── alert.js    町ぜんたいの警戒フェーズ
    ├── render.js   描画・ライティング
    ├── hud.js      HUD・レーダー
    └── audio.js    WebAudio（ノイズ源ベース）
```

- **画像・音声アセットは0件**。すべて Canvas 描画と WebAudio 合成
- 外部依存は Google Fonts のみ（v1から継続）

## 12. 動作確認の方法

```bash
_tools/preview.sh 120-konigokkogame     # ローカル配信（file:// では動かない）
node _tools/audit.mjs 120-konigokkogame  # 静的監査
_tools/game-harness.sh 120-konigokkogame # 動的検証
```

デバッグ用に `window.__KG` からゲーム状態にアクセスできる。`__KG.jumpTo(3)` で4面へ直接飛べる（0始まり）。
