# Hide and Seek — Stealth Escape

- 対象ゲーム: `120-konigokkogame`
- 作成日: 2026-08-29
- 更新日: 2026-08-30
- ステータス: 操作性・ビジュアル改善実装済み（実機確認待ち）
- 参照: タイタン指示（鬼ごっこ／小学生の帰宅／メタルギア風アウトプット／タイトルは英語の hide and seek）

注意: 番号 `120` は既存の `120-fumifumi` と衝突している。本フォルダ名は指示どおり維持。リネームは未確定。

## 1. ゲーム概要

- ジャンル: トップダウン・ステルス（かくれんぼ）
- 一言説明: 放課後の町で、鬼の懐中電灯を避けながら小学生が家まで帰る
- 想定プレイ時間: 1〜3分
- 想定プレイヤー: iPhone 片手プレイ
- クリア体験: 茂みと車に隠れ、警戒フェーズをやり過ごし、HOME の明かりに入る

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 対応画面幅: 320px〜430px
- 実装方式: HTML / CSS / JavaScript の静的ファイルのみ

### 任意
- PC: WASD / 矢印、Shift でしゃがみ

## 3. ファイル構成

### 必須
```text
120-konigokkogame/
  index.html
  style.css
  config.js
  stages.js
  draw.js
  script.js
  SPEC.md
  LEARNINGS.md
```

画像・音声ファイルは置かない（Canvas 描画と WebAudio）。

## 4. コアループ

1. タイトルで TAP TO START（ここで Audio unlock）
2. 短い作戦ブリーフィング（面ごとに内容が変わる）
3. 学校側から出発し、その面のゴール（GATE / ALLEY / PARK / STREET / HOME）へ向かう
4. 鬼の視界コーンに入ると警戒が上がる
5. 茂み・車では静止（または HIDE）しないと rustle で寄られる
6. TOSS で小石を投げる。着地点に人がいると勘違いした鬼が走り寄り、そこで探す。見つかっている相手は外れない
7. ゴール到達で次の面へ／捕まるか時間切れで同じ面をリトライ
8. ステージ5の HOME でキャンペーンクリア

5面: 1 校庭（鬼1）→ 2 商店街（鬼3）→ 3 路地（鬼3）→ 4 公園（鬼3・視界が広い）→ 5 家の通り（鬼4・玄関待ち）

バレたあとの追跡はプレイヤーより速い。開いた道を走って逃げ切れない。

警戒フェーズ: SAFE → CAUTION → ALERT（追跡）→ EVASION（捜索）→ SAFE

## 5. 画面構成と状態遷移

- タイトル → ブリーフィング → プレイ → 捕まった / 面クリア / 時間切れ / 全クリア
- 面クリア: TAP FOR NEXT → 次面のブリーフィング
- 失敗: TAP TO RETRY で同じ面を再開（タイトルへは戻らない）
- 全クリア: TAP TO TITLE
- プレイ中はポーズなし（任意・未実装）

## 6. 操作仕様

### 必須
- タッチ: 左下 D-pad、右下 TOSS（小石）と HIDE（押しっぱなしでしゃがみ）
- PC: WASD / 矢印、Shift = HIDE、Z / Space = TOSS
- 初回タップで開始と Audio unlock
- ダブルタップズーム防止、スクロール防止
- `pointerdown` で反応。仮想ボタンは `setPointerCapture`

## 7. ルールと勝敗条件

- 目的: 各面のゴールタイル（H）へ、鬼に捕まらず到達する
- 面クリア: H に入る（1–4は通過点、5は HOME）
- キャンペーンクリア: ステージ5の HOME
- 失敗: 隠れずに鬼へ接触、またはその面の制限時間切れ
- 隠れ中は至近距離以外は発見されない
- 光はルール。街灯の下は少し見えやすい。店先の漏れ光はわずかに見えやすい。暗い道は少し見えにくい。茂みの奥は暗い。発見の本体は懐中電灯のコーンだけ
- 店の窓は種類で色が違う（24H緑、ラーメン暖色）。消えた窓は暗い。光のたまりはポリゴンより先に町の空気を作る
- リトライ: 失敗時は同じ面。全クリア後のみタイトルへ

## 8. UI / HUD

- 上部: 警戒ゲージとフェーズ名、STAGE n/5、残り時間
- 右上: ソリトン風レーダー（自分・鬼・家）
- 右上端: ミュート
- 下部: D-pad と HIDE、safe-area を避ける
- 発見時: 頭上に `!` / `!!`

## 9. 音声仕様

### 必須
- TAP TO START で AudioContext 初期化
- BGM: オシレーターの低ドローン。ALERT 時はテンポと音程を変える
- SE: 発見、捕まった、帰宅、タップ
- ミュート: 画面上トグル、`localStorage` に保存
- `visibilitychange` / `pageshow` で resume を試行

## 10. アセット仕様

- 画像なし、音声ファイルなし
- 容量: コードのみ。20MB 鉄則に対して問題にならない想定

## 11. 保存 / 記録

- `localStorage`: ミュート状態のみ（キー `hide-and-seek-mute`）
- スコア保存なし

## 12. 実装ルール

- バニラ JS、`requestAnimationFrame`、iPhone 優先
- 1000 行超えたら同階層で分割
- `js/` `css/` `src/` は作らない

## 13. テストチェックリスト

- [x] タイトルから開始できる（Playwright）
- [x] ステージ1の校庭が出る（Playwright）
- [ ] ゴール到達で次面に進む
- [ ] 捕まったら同じ面をリトライする
- [ ] ステージ5の HOME で全クリアになる
- [x] D-pad／ドラッグスティックで歩ける（目的地10m→8mを確認）
- [ ] 茂みで隠れるとコーンから消える
- [ ] 接触で失敗になる
- [ ] ミュートが効く
- [x] 構文チェック通過（config / stages / draw / script）
- [ ] iPhone Safari 実機: 未検証

## 14. 未確定事項

- フォルダ番号を 120 のままにするか、空き番号へ移すか
- 実機 iPhone での確認（本セッションでは未実施）
- 100日チャレンジの Day 番号（投稿するか）

## 15. AI実装依頼用サマリー

- 目的: 小学生が放課後の町を隠れながら帰宅するステルス
- 入力: D-pad + HIDE、PC は WASD + Shift
- 画面: タイトル / 面ごとのブリーフィング / プレイ / 結果（次へ・リトライ・タイトル）
- 勝敗: 5面のゴールを順に踏む。失敗は同じ面
- UI: 警戒ゲージ、STAGE n/5、レーダー、仮想パッド、ミュート
- 音声: WebAudio のみ
- 見た目: 夕暮れの町＋メタルギア風の警戒UI（コーデック／ソリトン）

## 16. 操作性・ビジュアル改善（2026-08-30）

1. 十字ボタンにドラッグ式アナログ移動を追加（斜め・微入力対応）
2. 目的地の名前・距離・方向矢印を常時表示
3. HIDDEN / COVER / SEEKER CLOSE の状況表示
4. TOSSボタンにクールダウンリングと残り秒数を表示
5. ポーズ画面とステージ即リトライを追加
6. バックグラウンド移行時に自動ポーズ
7. ブリーフィングに3操作の視覚ガイドを追加
8. 残り20秒でタイマーを警告表示
9. 進行方向へ先読みするスムーズカメラを追加
10. ゴールの脈動枠・画面ビネット・操作ボタンの視覚フィードバックを強化
